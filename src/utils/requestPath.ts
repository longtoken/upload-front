const baseUrl = 'http://127.0.0.1:7001/';
const api = 'api/';
export let checkChunkPath = `${baseUrl}${api}checkChunk`;
export let uploadChunkPath = `${baseUrl}${api}uploadChunk`;
export let mergeChunkPath = `${baseUrl}${api}mergeChunk`;
