const baseUrl = 'http://127.0.0.1:7001/';
const api = 'api/';
export let checkChunk = `${baseUrl}${api}checkChunk`;
export let uploadChunk = `${baseUrl}${api}uploadChunk`;
export let mergeChunk = `${baseUrl}${api}mergeChunk`;
